# element-vir mono repo

See [packages](./packages) for each package.

Note: `npm i` currently crashes on this repo on deps regen for every npm version >11.10.0. To regenerate deps, temporarily revert npm to v11.10.0.
