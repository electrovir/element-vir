{
    "compilerOptions": {
        "module": "ES6",
        "moduleResolution": "node",
        // no implicit override is broken in snowpack
        "noImplicitOverride": false
    },
    "exclude": ["dist"],
    "extends": "./tsconfig-base.json"
}
